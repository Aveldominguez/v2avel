// Auto-incremented on each publish (+0.1 per release)
export const APP_VERSION = '3.21';

// Changelog for the current version — update this with each meaningful change
export const APP_CHANGELOG: string[] = [
  'ARION: departure_fn por proximidad temporal (≤6h tras la llegada).',
  'ARION: home_station guardada en cada vuelo sincronizado.',
  'Cabecera: origen→base para llegada y base→destino para salida.',
];
