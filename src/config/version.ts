// Auto-incremented on each publish (+0.1 per release)
export const APP_VERSION = '4.210';

// Changelog for the current version — update this with each meaningful change
export const APP_CHANGELOG: string[] = [
  'ITA Airways — A319: el modelo ya no muestra las casillas genéricas de bodegas de las primeras versiones de la app. Ahora tiene sus compartimientos reales, con el mismo formato de contenedores que el A320: delante 12 y 11 (puerta en la 11), detrás 42 (puerta) y 41, y el bulk 51 para carga a granel, sin contenedor y sin puerta de acceso.',
];
